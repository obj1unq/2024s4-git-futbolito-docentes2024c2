import objetos.*
import wollok.game.*

describe "futbolito" {
  test "inicio" {
    const inicio = game.at(0,5)
    assert.notEquals(inicio, lionel.position())
    lionel.inicio()
    assert.equals(inicio, lionel.position())
  }
}